//a program to calculate the bmi of a person 

//a would be used to represent the weight for ease 

//b would be used to represent the height for ease 

//let a = parseFloat(prompt("kindly input the weight of the individual; "));
let a = 40;
let b = 40;
//let b = parseFloat(prompt("kindly input the height of the individual; "));

bmi=(a/(b**2));

// if (bmi  )

alert(`the bmi is ${bmi}, i would get back to you on the bmi indicator shortly `)
